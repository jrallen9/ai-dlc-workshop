# Source Mirror

This directory separates untouched downloaded source-site files from curated website files.

- `raw/` contains the direct crawl output.
- `manifests/` contains crawl metadata, asset inventories, and link inventories.
- `cache/` and `tmp/` are local scratch directories and are ignored by Git.
